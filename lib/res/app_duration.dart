class AppDuration {
  static const Duration oneSecond = Duration(seconds: 1);
  static const Duration twoSecond = Duration(seconds: 2);
  static const Duration defaultDurationForSplashScreen = Duration(seconds: 3);
  static const Duration recipeOpenDuration = Duration(milliseconds: 300);
  static const Duration onBoardingScreenDuration = Duration(milliseconds: 300);
  static const Duration notificationPageOpenBlockDuration = Duration(milliseconds: 400);
  static const Duration swipeElementDuration = Duration(milliseconds: 300);
  static const Duration oneMinuteDuration = Duration(minutes: 1);
  static const Duration timeValidOfToken = Duration(hours: 1);
  static const Duration delayOfSendRequestToServer = Duration(minutes: 1);
}
